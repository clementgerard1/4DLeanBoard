import V_onePage from "./V_onePage.vue";
import V_player from "./V_player.vue";
import V_task from "./V_task.vue";

//Allow tests access classes
window.vueClasses = {
	V_onePage : V_onePage,
	V_player : V_player,
	V_task : V_task,
}